(ns cryptopals.core
  (:require [cryptopals.challenges.set4 :as set4]
            [cryptopals.utils :refer :all])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println (set4/challenge-31))
;  (println "Hello, World!")
  )
